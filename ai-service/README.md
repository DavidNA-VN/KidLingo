# AI Service

FastAPI service for doodle inference with the existing `j-doodle` checkpoint.

Current assets:

- `checkpoint.pt`: trained PyTorch checkpoint.
- `app/model.py`: `MobileNetV1` model definition copied from the original project.

The original Pygame UI is kept in `legacy/j-doodle/` for reference only.
